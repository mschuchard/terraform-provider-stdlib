---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "compare_string function - stdlib"
subcategory: ""
description: |-
  Compare two strings similar to the spaceship operator.
---

# function: compare_string

Lexicographically compares two strings and returns an integer indicating their relationship. A `-1` indicates the first string is lexicographically less than the second string, `0` indicates they are equal, and a `1` indicates the first string is lexicographically greater than the second string.

## Example Usage

```terraform
# Lexicographically compare two strings of equal length:
provider::stdlib::compare_string("aaa", "aab")
# result => -1

# Lexicographically compare two strings of unequal length:
provider::stdlib::compare_string("aaa", "aaaa")
# result => -1

# Lexicographically compare two strings of equal length:
provider::stdlib::compare_string("abc", "abc")
# result => 0

# Lexicographically compare two empty strings:
provider::stdlib::compare_string("", "")
# result => 0

# Lexicographically compare two strings of equal length:
provider::stdlib::compare_string("abd", "abc")
# result => 1

# Lexicographically compare two strings of unequal length:
provider::stdlib::compare_string("aaaa", "aaa")
# result => 1
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
compare_string(string_one string, string_two string) number
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `string_one` (String) First input string parameter to compare with the second.
1. `string_two` (String) Second input string parameter to compare with the first.
