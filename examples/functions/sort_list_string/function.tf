# Return the sorted list of integers cast as strings:
provider::stdlib::sort_list_string(["0", "4", "-10", "8"])
# result => ["-10", "0", "4", "8"]

# Return the sorted list of strings:
provider::stdlib::sort_list_string(["gamma", "beta", "alpha", "delta"])
# result => ["alpha", "beta", "delta", "gamma"]